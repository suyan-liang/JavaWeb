由于jQuery和JavaScript原来的差不多，而且样式太多，就不一一把自己的代码放上去了.....

只需记住，jQuery的关键在于如何获取到对象，而后用函数进行操作

脑子里面要有一颗DOM树，然后明白jQuery对象是一个DOM对象数组+提供的功能函数




jQuery数组中标签的顺序和页面中标签顺序一致


DOM对象用不了jQuery对象的方法，反之也用不了

一般js代码放在onload里面，就是页面加载完成之后运行，否则可能会出现没有效果的情况
